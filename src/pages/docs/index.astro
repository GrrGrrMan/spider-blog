---
// Redirect /docs to the canonical mission chapter
const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;

return Astro.redirect(`${base}docs/mission-and-intentions`);
---