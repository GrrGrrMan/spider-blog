---
// Canonical redirection: All software architecture is maintained under formal technical docs
const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;

return Astro.redirect(`${base}docs/system-overview`);
---