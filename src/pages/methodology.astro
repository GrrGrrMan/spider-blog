---
// Canonical redirection: Methodology consolidated into Technical Documentation & CAD Hub
const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;

return Astro.redirect(`${base}docs/mission-and-intentions`);
---